'use strict';
var expect = require('chai').expect;
var index = require('../dist/index.js');
describe('Empty Test', () => {
    it('should work', () => {
        expect(true).to.equal(true);
    });
});